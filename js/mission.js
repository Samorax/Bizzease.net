//document.addEventListener("DOMContentLoaded", MissionHandler, false);
/*function MissionHandler() {
    NavTextsRegistry("mission-nav","sections","fillMeUp");
}

function NavTextsRegistry(clickableClassName,replecableElementClassName,containerId) {
    /* When a clickable element is clicked, replace the elemet in container with the necessary paragraph. */
    /*get the clickable elements.
    var identifier = document.getElementsByClassName(clickableClassName);
    var idContainer = [];
    for (var j = 0; j < identifier.length; j++) {
        idContainer.push(identifier[j])
    };
    //get the replacing paragrahs.
    var paragraphs = document.getElementsByClassName(replecableElementClassName);
    var paragraphsContainer = [];
    for (var k = 0; k < paragraphs.length; k++) {
       paragraphsContainer.push(paragraphs[k])
    };
    
    //get container element
    var container = document.getElementById(containerId);
    
    for (var i = 0; i < identifier.length; i++) {
        identifier[i].addEventListener("click",function(event){
            event.preventDefault();
            var indexOfCurrentTarget = idContainer.indexOf(event.target);
            paragraphsContainer[indexOfCurrentTarget].style.display = "block";
            container.innerHTML = paragraphsContainer[indexOfCurrentTarget].innerHTML;            
        }, false);
            
    }
};
*/


    




